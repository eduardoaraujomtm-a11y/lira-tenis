// Interruptores de funcionalidades que existem no código mas ainda não devem
// aparecer para o público. Trocar para `true` religa tudo de uma vez.

/**
 * Ranking de atletas por pontos. Desligado enquanto os critérios de pontuação
 * do torneio não forem definidos — o cálculo já funciona, mas mostraria uma
 * classificação com regras provisórias.
 *
 * Ligando de novo: a aba volta ao menu, os atalhos em Resultados e Campeões
 * reaparecem e a página /ranking deixa de redirecionar.
 */
export const SHOW_RANKING = true;
